export enum FrequencyType {
    Daily = 'Daily',
    Weekly = 'Weekly',
    Monthly = 'Monthly',
    Yearly = 'Yearly'
}

export enum Gender {
    male,
    female
}

export enum Type {
    adult,
    child
}
